# Environment / secrets
.env
.env.local
secrets.env

# Logs / debug
webhook_debug.txt

# Composer dependencies (will be installed via Docker)
vendor/

# Node / frontend cache (optional)
node_modules/
dist/
